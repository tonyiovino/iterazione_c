#include <stdio.h>

int main()
{
  int a/*, b, c, d*/;

  /*(a, b, c, d)s = (a, b, c, d) + "s"pace */
  int as/*, bs, cs, ds*/;

  printf("Figura A:\n");
  for (a = 1; a <= 10; a += 1) {
    putchar('*');
    putchar('\n');
  }
  return 0;
}